@{
    Layout = "~/Views/Shared/_Layout.cshtml"; // Optional: Use your shared layout
    ViewBag.Title = "Success";
}

<div class="container">
    <div class="row">
        <div class="col-md-6 offset-md-3">
            <div class="card">
                <div class="card-body">
                    <h2 class="card-title">Operation Successful</h2>
                    <p class="card-text">@ViewData["SuccessMessage"]</p>
                    <a class="btn btn-primary" href="@Url.Action("Index", "Transactions")">Back to Transactions</a>
                </div>
            </div>
        </div>
    </div>
</div>
